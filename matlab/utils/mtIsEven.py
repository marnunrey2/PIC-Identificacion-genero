def mtIsEven(number):
    """
    mtIsEven
    Checks whether a number is even

    Parameters:
    - number (int): The number to check

    Returns:
    - isEven (bool): True if the number is even, False otherwise
    """
    return number % 2 == 0
